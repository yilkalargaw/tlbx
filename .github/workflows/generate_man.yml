name: Simple Readme Viewer

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  view-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show README.org
        run: |
          cd "$GITHUB_WORKSPACE"
          cat README.org
