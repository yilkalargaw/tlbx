name: Generate Man Page

on:
  push:
    paths:
      - 'README.org'
      - '.github/workflows/manpage.yml'
  workflow_dispatch:

permissions:
  contents: write  # Allows pushing changes

jobs:
  generate-manpage:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3  # Explicit version

      - name: Install pandoc
        run: sudo apt-get update && sudo apt-get install -y pandoc

      - name: Generate man page
        run: |
          mkdir -p man
          pandoc -s -t man README.org -o man/tlbx.1

      - name: Commit man page
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add man/tlbx.1
          git diff --cached --quiet || git commit -m "Auto-generated: Update man page from README.org"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Auto-generated token

      - name: Upload artifact
        uses: actions/upload-artifact@v3.1.3  # Fixed to a specific version
        with:
          name: tlbx-manpage
          path: man/tlbx.1
